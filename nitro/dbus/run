#!/bin/sh
/usr/bin/dbus-daemon --system --nofork --nopidfile 3>&- 2>&1 &
pid=$!
while [ ! -S /run/dbus/system_bus_socket ]; do
  if ! kill -0 "$pid" 2>/dev/null; then
    wait "$pid"
    exit 1
  fi
  sleep 0.05
done
printf '\n' >&3
exec 3>&-
wait "$pid"